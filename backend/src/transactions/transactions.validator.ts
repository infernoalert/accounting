// import { body, param } from 'express-validator';

// Placeholder
// We will add rules here later, e.g.:
// export const validateUpdate = [
//   param('id').isInt(),
//   body('category').optional().isString(),
//   body('subCategory').optional().isString(),
//   body('note').optional().isString(),
// ];
  